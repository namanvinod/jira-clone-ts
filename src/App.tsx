import './App.css';

const App = () => {
	const heading: string = 'Jira Clone';
	return (
		<div className="App">
			<header className="App-header">{heading}</header>
		</div>
	);
};

export default App;
